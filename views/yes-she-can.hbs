{{#if user}}
<div class="logger">
  {{!-- //APENAS USER TEM ACESSO --}}
  Olá, {{user.username}}

  {{#if isUser}}
  <a href="user/commit" class="btnAuth">Commit to us</a>
  {{/if}}

  {{!-- //APENAS ADM TEM ACESSO --}}
  {{#if isAdm}}
  <a href="adm/pull" class="btnAuth">Check pull</a>
  {{/if}}
</div>
{{/if}}


<div class="container-ysc">
  {{#each commit}}
  <div class="col-sm-3 card-ysc">
    <a href="{{this.url}}">
      <img class="card-img-top img-ysc" src="{{this.imgPath}}" alt=""></a>
    <div class="card-body">
      <p class="card-text">{{this.description}}</p>
      {{#if this.anonymous}}
      <p> postado por Anonimo</p>
      {{else}}
      <p> postado por {{this.owner.username}}</p>
      {{/if}}
    </div>
  </div>
  {{/each}}
</div>